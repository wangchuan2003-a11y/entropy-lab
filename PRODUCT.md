# Entropy Lab

中文优先的Ehrenfest双箱随机模型。N为20–300有标签粒子；每步均匀选一个粒子，独立以50%概率换箱、50%保持，明确为lazy变体。初始可全左或近似平衡，seed确定事件序列。提供播放、暂停、单步、倒退、回放、2000步硬上限，参数改变重开。

从同一轨迹显示两箱计数、粒子状态、S=log2 C(N,nLeft)、熵曲线与当前前缀经验直方图，和理论Binomial(N,0.5)对照。宏观多重度单位为bits，不是现实热力学熵。倒放使用已记录事件，不是自然逆转；有限相关采样不验证定理。

URL只保存初始设置，CSV保存当前回放前缀含初态。无空间/碰撞/温度/真实身份，无API或上传。ViteTS、Node22、PlaywrightCI；仅本地提交，root负责发布与内置浏览器QA，不得本机安装/启动独立浏览器。
