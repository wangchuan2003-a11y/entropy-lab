# 设计方向

以粒子实验仪器为中心：墨蓝背景、淡青标记、暖红选中粒子，双箱结构占首屏，设置沿上方排布。标记位置仅用于分清有标签粒子，不模拟空间。曲线与分布是真实模型输出，图表与计数共享当前轨迹光标。

粒子用确定性位置排布，换箱仅在x方向移动；重放不重新抽样。熵曲线保留起始点和当前前缀，不画虚构平滑曲线。直方图与理论曲线同轴，标注样本数，提供CSV用于复核。移动端上下堆叠，按钮最小44px，可键盘操作。减少动态时停止过渡动画。
